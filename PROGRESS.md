# Audit Trail Q&A System — Progress Tracker

> Keep this file at the root of your repo. Update it at the end of every work session (ask Claude Code to do it automatically — see instructions at the bottom). This is your single source of truth across devices.

**Last updated:** 2026-07-11
**Current phase:** Phase 9 — MCP Server (6 of 7 tools complete; `retrieve_similar_cases` blocked on Phase 8's outstanding similar-case retrieval)

---

## Status Legend
- [ ] Not started
- [~] In progress
- [x] Done

---

## Phase Checklist

### Phase 1 — Requirements Analysis
- [x] Business requirements defined
- [x] User roles identified (Auditor, Finance Manager, Admin)
- [x] Functional & non-functional requirements documented
- [x] Use cases written

### Phase 2 — High-Level Design (HLD)
- [ ] Architecture confirmed
- [ ] Tech stack finalized
- [ ] Service interaction diagram

### Phase 3 — Low-Level Design (LLD)
- [ ] Folder structure created
- [ ] DB schema drafted
- [ ] API contracts drafted

### Phase 4 — Database Design
- [x] PostgreSQL schema implemented
- [x] Relationships/foreign keys set
- [x] Seed synthetic ledger + audit data

### Phase 5 — Backend Foundation
- [x] FastAPI project scaffolded
- [x] JWT authentication
- [x] Role-based authorization
- [x] Logging + exception handling
- [x] Config management (.env, settings)

### Phase 6 — Ledger Service
- [x] Search & filter transactions
- [x] Pagination
- [x] Sorting
- [x] Input validation

### Phase 7 — Rule Engine
- [x] Duplicate detection
- [x] Threshold violation
- [x] Round-number detection
- [x] Debit/credit validation
- [x] Vendor-account mismatch (pairing)
- [x] Split payment detection
- [x] Weekend/holiday transactions
- [x] Missing approval
- [x] Inactive vendor
- [x] Benford's Law analysis (batch/aggregate, not per-transaction — see decisions log)
- [x] Risk scoring engine

### Phase 8 — RAG Layer
- [x] Policy PDF chunking
- [x] Embeddings (sentence-transformers)
- [x] Vector DB setup (Qdrant/Chroma)
- [x] Policy retrieval
- [ ] Similar case retrieval

### Phase 9 — MCP Server
- [x] query_ledger()
- [x] flag_discrepancy()
- [x] generate_audit_note() (placeholder — real drafting is Phase 10)
- [x] retrieve_policy()
- [ ] retrieve_similar_cases() (blocked on Phase 8's outstanding similar-case retrieval)
- [x] risk_score()
- [x] export_report() (placeholder — later-phase Reporting Service feature)

### Phase 10 — LLM Integration
- [ ] Prompt templates
- [ ] Claude API integration
- [ ] JSON structured output parsing
- [ ] Audit note generator service

### Phase 11 — Frontend
- [ ] Login page
- [ ] Dashboard
- [ ] Ledger Explorer
- [ ] Transaction Details
- [ ] Audit Workspace
- [ ] Policy Search
- [ ] Case Library
- [ ] Reports
- [ ] Admin Panel

### Phase 12 — Testing
- [ ] Unit tests
- [ ] Integration tests
- [ ] API tests
- [ ] RAG evaluation

### Phase 13 — Deployment
- [ ] Dockerfiles (backend, frontend)
- [ ] docker-compose
- [ ] Nginx config
- [ ] CI/CD (GitHub Actions)
- [ ] Monitoring (optional — Prometheus/Grafana)

---

## What's done (running log)
_(Append a dated bullet each session)_

- 2026-07-09: Completed docs/requirements.md — defined the 3 user roles (Auditor, Finance Manager, Admin), 9 functional requirements, and 4 non-functional requirements.
- 2026-07-10: Config management (.env, settings) completed. PostgreSQL is now running via docker-compose and verified connected.
- 2026-07-10: Created all 11 SQLAlchemy models (backend/app/models/) with relationships (transactions→vendors/departments, audit_flags→transactions, audit_notes→audit_flags, users→roles, approval_limits→departments/roles, audit_cases/policies for RAG, audit_logs as immutable append-only). Set up Alembic, generated and applied the initial migration against the docker-compose Postgres instance — verified all 11 tables created with no autogenerate drift.
- 2026-07-10: Database schema and relationships confirmed live: migration re-verified against Postgres, all 11 tables present with FKs matching the models. Seed data still outstanding — Phase 4 not fully closed.
- 2026-07-10: Implemented JWT authentication (Phase 5 complete). Added `POST /api/v1/auth/login` (email/password → access token) and `GET /api/v1/auth/me`; bcrypt password hashing via passlib (`app/auth/security.py`); `get_current_user` and `require_role(*role_names)` dependencies (`app/auth/dependencies.py`, case-insensitive role matching); `scripts/seed_users.py` creates one test user per role (Auditor, Finance Manager, Admin — password `ChangeMe123!`), idempotent. Added `JWT_SECRET_KEY`/`JWT_ALGORITHM`/`ACCESS_TOKEN_EXPIRE_MINUTES` to `.env`/`.env.example`. Verified end-to-end against the live server: login, `/auth/me`, wrong-password rejection, missing/garbage/expired token rejection (401), and role-based 403 rejection all confirmed working.
- 2026-07-10: **Phase 5 — Backend Foundation fully complete.** Re-verified the auth router is correctly wired in `app/main.py` (`app.include_router(auth_router, prefix=settings.api_v1_prefix)`) by starting the server fresh and manually exercising every case end-to-end: `POST /api/v1/auth/login` with valid credentials (200 + token), `GET /api/v1/auth/me` with a valid token (200), wrong password (401), missing token (401), garbage token (401), and expired token (401). All five Phase 5 items (FastAPI scaffold, JWT auth, role-based authorization, logging + exception handling, config management) confirmed working together.
- 2026-07-11: **Phase 6 — Ledger Service complete.** Added `GET /api/v1/transactions` (filter by date range, vendor, department, amount range, status; sort by transaction_date/amount/risk_score/created_at asc/desc; paginated with page/page_size, capped at 100/page) and `GET /api/v1/transactions/{id}` (404 if missing, includes nested vendor/department detail via `joinedload`) in `app/api/transactions.py`. Both endpoints require a valid JWT (any authenticated role) via the existing `get_current_user` dependency — no additional role restriction. Filtering/sorting/pagination/cross-field validation (date_from ≤ date_to, amount_min ≤ amount_max) is driven by a single Pydantic model, `TransactionFilterParams` (`app/schemas/transaction.py`), bound as query params via FastAPI 0.115's native "query parameter models" support (`Annotated[TransactionFilterParams, Query()]`) rather than individual `Query(...)` args. Also wrote `scripts/seed_transactions.py`: idempotently seeds 8 departments and 17 vendors (2 inactive, for the future "inactive vendor" rule), then inserts 260 transactions — 236 baseline + 10 round-number anomalies + 6 exact-duplicate-payment anomalies + 8 threshold-violating anomalies (>$10,000) — for Phase 7's rule engine to detect. Verified end-to-end against a live server: unauthenticated request (401), login as seeded auditor, list with default pagination (260 total / 25 per page / 11 pages), filter by amount_min+sort_by=amount (correctly surfaced all 8 threshold anomalies plus round-number 15000s), filter by department+status, filter by date range, detail endpoint with nested vendor/department, 404 on unknown id, and validation errors (amount_min > amount_max, date_from > date_to, invalid enum, page_size > 100, unknown query param) all return clean 422s.
- 2026-07-11: **Bug fix (pre-existing, surfaced by Phase 6 work):** `app/middleware/exception_handler.py`'s `RequestValidationError` handler was returning HTTP 500 instead of 422 whenever a Pydantic `@model_validator` raised a `ValueError` — `exc.errors()` embeds the raw exception instance in `ctx["error"]`, which `JSONResponse`'s `json.dumps` can't serialize. Fixed by stringifying `ctx["error"]` before building the response body. This was latent since Phase 5 (no schema had used a cross-field `model_validator` yet) and will affect any future validator that does the same.
- 2026-07-11: **Bug fix: `scripts/seed_transactions.py` was not idempotent.** It always appended a fresh batch of 260 transactions on top of whatever existed (continuing `transaction_ref` from the current max), so running it across two sessions left 520 rows instead of 260. Cleared the 520 duplicated rows from the live `transactions` table (`DELETE FROM transactions` — verified `audit_flags`/`audit_cases`/`audit_notes` were empty first, so no orphaned FK references). Fixed the script: it now deletes all existing transactions (`clear_transactions()`, a plain bulk `DELETE`, not `TRUNCATE ... CASCADE`, so it fails loudly instead of silently wiping dependent audit tables if Phase 7/8 ever populate them) and always re-inserts exactly `TARGET_TRANSACTION_COUNT` (260) fresh rows with refs starting at `TXN-000001`. Verified by running it twice in a row: second run printed "Cleared 260 existing transactions before reseeding" and the table still held exactly 260 rows afterward.
- 2026-07-11: **Phase 6 — Ledger Service and Phase 4's remaining seed-data item both confirmed fully closed.** Re-verified `GET /api/v1/transactions` filtering by every documented query param — `amount_min`/`amount_max`, `date_from`/`date_to`, `vendor_id`, `department_id`, `status` — plus `sort_by`/`sort_order`/`page`/`page_size`, and re-confirmed cross-field validation (`amount_min > amount_max`, `date_from > date_to`) returns clean 422s, not 500s. `scripts/seed_transactions.py` re-confirmed idempotent at exactly 260 rows after the duplicate-seeding fix above.
- 2026-07-11: **Phase 7 — Rule Engine started: duplicate detection, threshold violation, and the risk scoring engine.** Added `app/rules/duplicate.py` (flags transactions matching another on vendor_id + department_id + amount within a configurable `±duplicate_detection_window_hours`, default 24h) and `app/rules/threshold_violation.py` (flags amount > configurable `threshold_violation_amount`, default $10,000) — both new settings in `app/core/config.py`/`.env.example`, and `scripts/seed_transactions.py` now imports `settings.threshold_violation_amount` instead of its own hardcoded copy of the same number. Each rule module returns a `RuleFinding(rule_name, risk_points, details)` (`app/rules/base.py`); `app/rules/__init__.py` exposes `ACTIVE_RULES = [duplicate, threshold_violation]` as the registry future rule modules get added to. Risk scoring lives in `app/services/risk_scoring.py`: `evaluate_transaction()` runs every active rule, upserts `audit_flags` (updates an existing OPEN flag's `risk_points`/`details` in place if the rule still fires, resolves it with `status=RESOLVED`+`resolved_at` if it stops firing, so re-running never piles up duplicate flag rows), sums `risk_points` into `transactions.risk_score`, and buckets it into `risk_level` via score bands (≥75 CRITICAL, ≥50 HIGH, ≥25 MEDIUM, ≥1 LOW, 0 → null) — using duplicate=40/threshold=30 per docs/design.docx's example weights. Added two endpoints in `app/api/rule_engine.py`, both behind the existing JWT `get_current_user` dependency (no extra role restriction, matching the Phase 6 ledger endpoints): `POST /api/v1/transactions/{id}/evaluate` (re-evaluates one transaction, returns its updated risk_score/risk_level/open flags, 404 if missing) and `POST /api/v1/transactions/evaluate-all` (batch-evaluates every transaction, returns evaluated/flagged counts and a risk_level breakdown). Verified end-to-end against the live server and the seeded 260-row dataset: `evaluate-all` correctly flagged exactly 24 transactions (12 `duplicate` = all 6 seeded duplicate pairs, 12 `threshold_violation` = the 8 dedicated threshold anomalies plus 4 of the round-number-anomaly transactions that happened to land on $15,000, which legitimately also exceeds the $10k threshold) — cross-checked directly against `audit_flags`/`transactions` via psql, all `risk_score`/`risk_level` values correct (30/MEDIUM, 40/MEDIUM). Confirmed idempotency by re-running `evaluate-all` immediately after: identical counts, no duplicate flag rows. Confirmed the resolve path by manually changing one duplicate counterpart's amount, re-evaluating: `risk_score` dropped to 0, the old flag flipped to `RESOLVED` (kept, not deleted) — then restored the amount and re-ran `evaluate-all`, which correctly created a fresh `OPEN` duplicate flag and brought the count back to 24.
- 2026-07-11: **Bug fix: `status=flagged` ledger filter didn't surface rule-engine-flagged transactions, and there was no way to filter by risk_level at all.** User reported `evaluate-all` reporting `flagged_count=24` while `GET /transactions?status=flagged` only returned 7, with `risk_score=0`/`risk_level=null` on those 7. Root cause (confirmed directly in Postgres, not a persistence bug — `evaluate-all` was correctly writing `risk_score`/`risk_level` all along): `transactions.status` (`pending`/`approved`/`rejected`/`flagged`/`cleared`) is a workflow field randomly assigned by the seed script, completely independent of `risk_score`/`risk_level`, which only the rule engine sets — the shared word "flagged" was a coincidence, not a real link. Notably 18 of the 24 rule-engine-flagged transactions currently have `status=APPROVED` or `CLEARED`. Asked the user whether the rule engine should also set `status=FLAGGED` on `risk_score > 0` (which would silently overwrite those 18 prior approvals) — they chose **not** to couple the two; `status` stays a purely human/workflow field. Fix: added `risk_level` as a `TransactionFilterParams` field (`app/schemas/transaction.py`) and wired it into `GET /transactions` (`app/api/transactions.py`). Also fixed a knock-on issue this surfaced: `scripts/seed_transactions.py`'s `clear_transactions()` started failing with a `ForeignKeyViolation` once `audit_flags` rows existed (from running `evaluate-all`), since it deliberately doesn't cascade — now clears `audit_flags` first (safe: it's entirely rule-engine-derived, regenerated by re-running evaluate-all) while still leaving `audit_cases`/`audit_notes` alone. Re-verified end-to-end on a fresh reseed: `evaluate-all` → 24 flagged → `GET /transactions?risk_level=medium` returns exactly those 24 with correct `risk_score`/`risk_level` on every row, composes correctly with `sort_by=amount`, rejects an invalid `risk_level` value with 422, and `status=flagged` still correctly returns its unrelated 7.

- 2026-07-11: **Phase 7 — Rule Engine complete: all 10 design.docx rules implemented, wired into risk scoring, and verified against the seeded data.** Added the remaining 7 per-transaction rule modules — `round_number.py` (5 pts, amount is an exact multiple of $100), `debit_credit_validation.py` (10 pts, a vendor payment recorded as CREDIT instead of DEBIT — this ledger only records outgoing vendor payments, so CREDIT is inherently suspect), `vendor_account_mismatch.py` (15 pts, `transaction.account_number` ≠ `vendor.bank_account_number`), `split_payment.py` (35 pts, ≥`split_payment_min_count` (default 3) transactions to the same vendor within `±split_payment_window_hours` (default 72h) summing to at/above the threshold while each stays under it — structuring), `weekend_holiday.py` (10 pts, `transaction_date` falls on Sat/Sun or a date in the new configurable `settings.holidays` list), `missing_approval.py` (20 pts, status is APPROVED/CLEARED but `approved_by_id` is null), `inactive_vendor.py` (20 pts, linked vendor has `is_active=false`) — all registered in `app/rules/__init__.py`'s `ACTIVE_RULES`, so `evaluate_transaction()`/`evaluate-all` automatically run all 9 per-transaction rules with no other code changes needed. Point values: duplicate=40, threshold=30, vendor_account_mismatch=15, weekend_holiday=10, round_number=5 come directly from docs/design.docx's example weights; debit_credit_mismatch=10, missing_approval=20, inactive_vendor=20, split_payment=35 are inferred (design.docx's example only weights 5 of the 10 rules) — logged below. Added `benfords_law.py` as a **separate, population-level** batch analysis (chi-square test on leading-digit distribution vs. the expected Benford frequencies) — deliberately NOT in `ACTIVE_RULES` and never written to any transaction's `risk_score`/`audit_flags`, since no single transaction can "violate" a statistical property of a whole sample (matches the task's own note that this is "most useful as a batch/aggregate check"); it runs once per `evaluate-all` call over the whole evaluated set and is returned as a `benfords_law` field in `BatchEvaluationOut`. Extended `scripts/seed_transactions.py` with deliberate, isolated anomalies for every new rule (6 debit/credit mismatches, 6 vendor-account mismatches, 6 missing-approval, 2 split-payment groups of 3, 4 holiday-dated) and made baseline transactions realistic instead of uniform-random noise (`debit_credit` always DEBIT, `account_number` always matches the vendor's `bank_account_number`) — without this, ~50% of the old uniform-random baseline would've false-positived on the new debit/credit and vendor-account rules, making verification meaningless. `TARGET_TRANSACTION_COUNT` raised from 260 to 288 to fit the new anomalies (still within the original 200–300 spec) — inactive_vendor and weekend hits are *not* separately seeded since they already occur naturally and in reasonable volume from the existing random vendor/date selection (~34 and ~104 respectively). Verified end-to-end on a fresh reseed + `evaluate-all`: 288 evaluated, 159 flagged, all 9 rules' flag counts cross-checked directly in Postgres — round_number (10, all exact $100 multiples), debit_credit_mismatch (6, all CREDIT), vendor_account_mismatch (6, `account_number` confirmed ≠ vendor's), missing_approval (6, confirmed null `approved_by_id` on APPROVED/CLEARED rows), inactive_vendor (34, all linked to the 2 inactive vendors), weekend_holiday (104, spot-checked weekday-of-week including the exact holiday-reason message on a Friday holiday date), threshold_violation/round_number/duplicate counts all cross-checked as before. Spot-checked compound scoring on multiply-flagged transactions: 40(duplicate)+35(split_payment)=75=CRITICAL, 30(threshold)+5(round_number)+10(weekend_holiday)+20(inactive_vendor)=65=HIGH, both exactly correct. Confirmed idempotent by re-running `evaluate-all`: identical counts, no duplicate rows.
- 2026-07-11: **Bug fix (found during this session's own verification, not user-reported): `scripts/seed_transactions.py`'s duplicate-anomaly generator could chain a clone into another clone's source.** It picked `source = random.choice(transactions)` from the list *as it grew*, so a later duplicate-anomaly iteration could occasionally pick an earlier iteration's clone as its own source, silently turning two separate intended pairs into one 3-way cluster plus a missing pair — and since the cluster shared one vendor within a tight window, it also cross-triggered `split_payment` as a side effect. Fixed by freezing `duplicate_source_pool = list(transactions)` once, before the anomaly loop starts, so a clone can never be selected as another pair's source. (A *coincidental* 3-way cluster can still occur if two independent random picks from the frozen pool land on the same original transaction — confirmed this is legitimate sampling variance, not a bug, by checking that in that case all members do genuinely share identical vendor/department/amount/date and are correctly flagged as a real 3-way duplicate group.)
- 2026-07-11: **Phase 8 — RAG Layer: policy PDF chunking, embeddings, Qdrant storage, and retrieval endpoint built and verified against the 3 real RBI Master Direction PDFs in `documents/policies/`.** Added Qdrant to `docker-compose.yml` (persistent `qdrant_data` volume, port 6333/6334). Discovered `docs/design.docx`/`docs/roadmap.docx` are plain text despite the `.docx` extension (not real Office XML) — read directly rather than via a docx parser. Built `app/services/policy_chunking.py`: chunks each PDF by its actual chapter/clause structure (e.g. "4.1.5") instead of fixed-size splitting, using `pdfplumber`. Key technique: footnotes/citation superscripts render at a smaller point size (~8pt) than body text (10-12pt) in all 3 source PDFs, so `page.filter()` strips sub-9.5pt characters before line reconstruction — this avoids footnote lines like "22 Amended vide circular ..." being mistaken for a numbered clause "22". Also detects and skips table-of-contents pages/spillover (heading lines ending in a bare page number), dedupes clauses that the two Fraud Risk Master Directions restate verbatim in their own TOC (kept longest content per `(chapter, clause_ref)`, not clause_ref alone, since KYC's flatter numbering can legitimately restart per chapter), and cuts each document off at its trailing boilerplate "Appendix: List of Circulars Repealed" table (real content, just not policy substance — its numbered rows were fragmenting into dozens of junk chunks). Added `chapter`/`clause_ref` columns to `Policy` (migration `4ec0911d45e2`) alongside the existing `document_name`/`content`/`source_page`. `scripts/ingest_policies.py` chunks all 3 PDFs, stores chunks in Postgres, embeds them (`app/services/embeddings.py`, singleton-cached `SentenceTransformer`), and upserts into a Qdrant `policies` collection (cosine distance) keyed by `Policy.id`; fully idempotent (clears/recreates both Postgres rows and the Qdrant collection every run). Added `POST /api/v1/policies/search` (`app/api/policies.py`, `app/services/policy_search.py`), behind `get_current_user` like the other Phase 6/7 endpoints — embeds the query and returns Qdrant's top-k hits with score/document/chapter/clause/page/content. 436 raw chunks extracted, 322 after the appendix-table cutoff.
- 2026-07-11: **Retrieval-quality tuning, driven by the task's own verification queries.** Initial pipeline (BAAI/bge-small-en-v1.5, clause text embedded alone) buried the correct clause for "what is the reporting threshold for fraud in NBFCs" at rank 19/20 — clause 6.2.1 ("...irrespective of the amount involved...") shares almost no vocabulary with the query since it never uses the word "report"; its parent clause headings (6.1/6.2, both literally titled "Reporting ... to RBI") do. Two fixes, both in `scripts/ingest_policies.py`: (1) switched the embedding model to **BAAI/bge-base-en-v1.5** (`app/core/config.py` `embedding_model_name`) — larger/more accurate, still fully local; (2) `_build_embedding_texts()` prepends each chunk's ancestor clause headings (walking `clause_ref` up by dropping the last `.N` segment) plus its chapter label to the text handed to the embedder — the stored/returned `content` is untouched, only the embedding input is enriched. Together these moved the correct clause from rank 19 to within the top 8 default results; a second query ("who approves fraud classification") and a third ad-hoc query against the KYC document both correctly return the right document and clause within the top few results without needing this treatment, confirming the fix targets a genuine gap rather than being over-fit to one query. `policy_search_default_top_k` raised from 5 to 8 to comfortably fit real answers for terse/negation-phrased queries; callers can still override `top_k` (1-20) per request.
- 2026-07-11: **Phase 9 — MCP Server: 6 of 7 tools built in `app/mcp/server.py` using the MCP Python SDK's `FastMCP`, each wrapping existing service-layer logic rather than reimplementing it.** Hit an immediate dependency conflict installing `mcp[cli]`: it resolves to `starlette>=0.49`/`sse-starlette>=3` by default, but `fastapi==0.115.0` requires `starlette<0.39.0` and breaks outright (`Router.__init__() got an unexpected keyword argument 'on_startup'`) on the newer one. Fixed by skipping the `[cli]` extra (its SSE/HTTP-transport deps aren't needed for a stdio MCP server anyway) and pinning `starlette==0.38.6`/`sse-starlette==2.1.3` — both still satisfy `mcp`'s own loose lower bounds (`starlette>=0.27`, `sse-starlette>=1.6.1`). `mcp` also bumped `pydantic`, `pyjwt`, and `uvicorn` past their old pins; re-verified the full existing app (login, `/auth/me`, transactions list/detail/evaluate, policy search) end-to-end afterward with no regressions, then re-pinned `requirements.txt` to the versions actually resolved (same pattern as Phase 8's qdrant-client pin). To avoid duplicating query/lookup logic between the FastAPI routes and the MCP tools, extracted `app/services/ledger_service.py` (`search_transactions`, `get_transaction_by_id` — previously inlined in `app/api/transactions.py`) and moved `app/api/rule_engine.py`'s private `_open_flags` helper into `app/services/risk_scoring.py` as `get_open_flags`; both routes and the new MCP tools now call the same functions. Tools: `query_ledger` (flat primitive params → builds a `TransactionFilterParams`, catches `ValidationError` and returns a clean `{"error": ...}` dict instead of a raw traceback) and `retrieve_policy` wrap the existing Phase 6/8 services directly; `risk_score` is read-only (returns already-computed `risk_score`/`risk_level`/open flags, does not re-run the rule engine); `flag_discrepancy` is new logic (no prior manual-flag endpoint existed) added as `create_manual_flag()` in `risk_scoring.py`; `generate_audit_note`/`export_report` are the task's requested placeholders, returning a `{"status": "not_implemented", ...}` payload (the former still 404s on an unknown `transaction_id` rather than silently no-opping). MCP tools intentionally do **not** go through `get_current_user`/JWT — there's no HTTP request/Authorization header in an MCP tool call, and the MCP host process itself is the trust boundary (matches how local stdio MCP servers are normally run). Verified via `FastMCP.list_tools()`/`call_tool()` in-process (no subprocess/client needed for local testing): all 6 tools listed with correct JSON schemas; `query_ledger` with `risk_level=critical` correctly returned 3 real transactions; `risk_score` on a real duplicate+split_payment transaction (id 1485) correctly returned `risk_score=75`/`critical` with both flags; `retrieve_policy("who approves fraud classification")` correctly returned the commercial-banks MD's clause 2.1.1.4; `flag_discrepancy` created a manual flag (confirmed `risk_score` stayed at 75 afterward, not corrupted by the 0-point manual flag) then cleaned up; `query_ledger` with `amount_min > amount_max` correctly returned a clean validation error instead of crashing; `generate_audit_note`/`export_report` returned their placeholder payloads. Test artifacts (manual test flag, temp response files) cleaned up after verification.

## What's next (top priority, always keep this current)
1. Phase 8/9 — `retrieve_similar_cases` MCP tool + Phase 8's similar-case retrieval it depends on (backed by the existing `audit_cases` table) — the one remaining gap in both phases.
2. Phase 10 — LLM Integration: prompt templates, Claude API integration, and filling in the `generate_audit_note` MCP tool (currently a placeholder) with real structured-output drafting.
3. Longer-term (not blocking): revisit `app/services/risk_scoring.py`'s risk-level score bands now that all 9 per-transaction rules can stack (max 185 points) — still monotonic and behaving sensibly per prior verification, but worth a deliberate pass once real usage patterns (not just synthetic seed data) are available.
4. Longer-term (not blocking): retrieval for negation/threshold-style queries (e.g. "what is the threshold for X") still ranks the exact answer clause below topically-related neighbors rather than #1 — acceptable given it lands within the default top-8, but a cross-encoder reranking pass over the top-N vector-search candidates (tested `BAAI/bge-reranker-base` ad hoc; didn't clearly fix this specific case either) or hybrid BM25+vector search would be the next thing to try if this proves to matter in practice.
5. Longer-term (not blocking): `export_report` and `generate_audit_note` are still placeholders per this session's task — real implementations land in Phase 10 (audit notes) and whenever the Reporting Service (design.docx backend module 9) is scheduled.

## Blockers / open questions
- MCP server (`app/mcp/server.py`) has no auth/identity layer — it currently relies entirely on a stdio-only trust model (the MCP host process that spawns it is treated as the trust boundary; no JWT/role check like the HTTP API has, and tool calls are anonymous). Before Phase 13 deployment, or before exposing this over any non-stdio transport (SSE/HTTP), need to: add per-call identity via FastMCP's `Context`, add a `created_by` column to `AuditFlag`, wire state-changing tool calls (currently just `flag_discrepancy`) into `audit_logs`, and decide whether to tier tool access by risk (read-only vs. write). Not a blocker for current local development.

## Key decisions log
_(Record anything you decided that deviates from the original design doc, so future-you knows why)_

- design.docx lists table names only, no columns — column design was inferred from requirements.md and the rule engine list (Phase 7) in design.docx.
- `transactions.risk_score`/`risk_level` are denormalized columns (not computed on the fly) so ledger search/filtering by risk stays index-backed per the NFR-4 performance requirement; the rule engine (Phase 7) will write to them.
- `audit_flags.rule_name` is a plain indexed string, not a DB enum, so new rule modules can be added without a migration.
- `audit_notes.cited_policy_ids`/`cited_case_ids` are JSON arrays of IDs rather than join tables — keeps the explainability citation list simple; may revisit as a proper many-to-many if we need to query "which notes cite policy X" efficiently.
- `audit_logs` has no `updated_at` and is treated as insert-only at the ORM level (NFR-2 immutability) — nothing currently enforces this at the DB level (e.g. no revoked UPDATE/DELETE grants); revisit if that becomes a compliance requirement.
- `/auth/login` accepts a JSON body (`email`/`password`) rather than OAuth2 form-encoded fields — simpler for a JSON-only API; `OAuth2PasswordBearer` is still used purely to extract the `Authorization: Bearer` header on protected routes, so Swagger's "Authorize" button still works for testing.
- `bcrypt` pinned to `4.0.1` alongside `passlib==1.7.4` — newer bcrypt (4.1+) removed an attribute passlib's version probe reads, which breaks hashing at import time.
- `require_role(*role_names)` matches role names case-insensitively (`casefold()`) since `roles.name` is admin-editable free text, not a fixed enum.
- Ledger endpoints (`GET /transactions`, `GET /transactions/{id}`) are open to any authenticated role (just `get_current_user`, no `require_role`) per the Phase 6 task — viewing the ledger isn't role-restricted, only actions like approving/flagging will be later.
- `TransactionFilterParams` is a Pydantic model bound directly as query params (FastAPI 0.115's "query parameter models": `Annotated[TransactionFilterParams, Query()]`), not individual `Query(...)` function args — keeps cross-field validation (date range, amount range) in one place via `@model_validator`, and `extra="forbid"` rejects unknown query params instead of silently ignoring them.
- `scripts/seed_transactions.py` uses a fixed `THRESHOLD_AMOUNT = 10000.00` purely as a reference point for generating "obviously too large" anomaly transactions — it does not seed `approval_limits` rows. Actual per-department/role thresholds for the Phase 7 rule engine should be seeded separately (or read from `approval_limits`) when that rule is implemented.
- Rule modules (`app/rules/*.py`) share a plain interface — `RULE_NAME: str` + `evaluate(transaction, db) -> RuleFinding | None` — rather than a class hierarchy; `app/rules/__init__.py`'s `ACTIVE_RULES` list is the only place a new rule module needs to be registered. Chosen over an ABC/plugin registry since 10 total rules (per design.docx) doesn't justify the extra machinery.
- `evaluate_transaction()` (`app/services/risk_scoring.py`) upserts `audit_flags` instead of always inserting a fresh row: an existing `OPEN` flag for a still-triggering rule gets its `risk_points`/`details` updated in place, and a flag whose rule stops triggering is marked `RESOLVED` (not deleted) — keeps `POST .../evaluate` and `.../evaluate-all` safe to call repeatedly (same lesson as the seed-script idempotency bug above) while preserving audit history instead of silently losing it.
- `threshold_violation_amount` (default $10,000) and `duplicate_detection_window_hours` (default 24h) are configurable via `Settings`/`.env`, not hardcoded in the rule modules, per the task's "configurable amount threshold" requirement. `scripts/seed_transactions.py` now reads the same `settings.threshold_violation_amount` instead of its own copy of the number, so the seed data and the rule that scores it can't drift apart.
- Risk-level score bands (`_RISK_LEVEL_BANDS` in `app/services/risk_scoring.py`: ≥75 CRITICAL, ≥50 HIGH, ≥25 MEDIUM, ≥1 LOW) are inferred, not specified in design.docx — it only gives one example (duplicate 40 + threshold 30 + vendor 15 + weekend 10 + round-number 5 = 100 → Critical). Revisited now that all 9 per-transaction rules are active (max possible 185, up from 70): left unchanged after this session's verification — they're still monotonic and produced sensible results against the seeded data (e.g. 40+35=75 correctly landed CRITICAL, 30+5+10+20=65 correctly landed HIGH), so no evidence yet that they need adjusting. Worth a deliberate re-look once real (non-synthetic) usage data exists.
- Rule engine / risk scoring endpoints (`POST /transactions/{id}/evaluate`, `POST /transactions/evaluate-all`) use `get_current_user` only, no `require_role` — matches the Phase 6 ledger endpoints' precedent (view/compute actions open to any authenticated role); revisit if the roadmap later restricts who can trigger re-scoring.
- `transactions.status` (workflow: pending/approved/rejected/flagged/cleared) and `risk_score`/`risk_level` (computed by the rule engine) are deliberately **not** coupled — `evaluate_transaction()` never touches `status`. Considered auto-setting `status=FLAGGED` when `risk_score > 0`, but that would silently overwrite transactions a human had already marked `APPROVED`/`CLEARED`; user explicitly chose to keep `status` as a purely human/workflow field. Filtering for rule-engine findings should use the `risk_level` ledger filter (added this session), not `status=flagged`.
- `scripts/seed_transactions.py`'s `clear_transactions()` also deletes `audit_flags` before deleting transactions (not just transactions) — once the Phase 7 rule engine has run, `audit_flags` rows reference transactions and a plain DELETE on `transactions` alone raises a `ForeignKeyViolation`. Safe because `audit_flags` is entirely rule-engine-derived output (regenerated by re-running `evaluate-all`); `audit_cases`/`audit_notes` are still deliberately left alone.
- Point values for the 5 rules design.docx doesn't weight in its example (debit_credit_mismatch=10, missing_approval=20, inactive_vendor=20, split_payment=35) are inferred, not specified — design.docx's "Total = 100" example only weights duplicate/threshold/vendor/weekend/round-number. Reasoning: split_payment (35) is weighted close to duplicate/threshold since deliberate structuring to dodge an approval control is as serious as the threshold violation it's evading; missing_approval/inactive_vendor (20 each) are real control gaps, weighted above the "soft" signals (round_number=5, weekend_holiday=10) but below outright evasion; debit_credit_mismatch (10) is the weakest signal here since a CREDIT against a vendor could be a legitimate reversal, not just miscoding.
- `benfords_law.py` is **not** a per-transaction rule and is deliberately excluded from `ACTIVE_RULES` — Benford's Law is a statistical property of a whole sample's leading-digit distribution, so no single transaction can "violate" it the way it can be over the threshold or a duplicate. It's run once per `evaluate-all` call (chi-square test, 8 degrees of freedom, p=0.05 critical value ≈15.507) over the whole evaluated batch and returned as a separate `benfords_law` field, never written to any transaction's `audit_flags`/`risk_score`. Note for future sessions: the synthetic seed data is generated via `random.randint()` (uniform), which does **not** naturally follow Benford's Law the way real transaction amounts do (real amounts follow it due to multiplicative/log-scale pricing processes) — so `evaluate-all` against the seed data will always report `is_significant: true`. That's expected and correct given how the data is generated, not a sign the seed data or the check is broken; it just means this particular check can't be meaningfully sanity-tested against synthetic uniform-random amounts.
- `scripts/seed_transactions.py` baseline transactions were changed from uniform-random `debit_credit`/`account_number` to realistic-and-clean (`debit_credit=DEBIT` always, `account_number` always equal to the vendor's `bank_account_number`) once the debit_credit_mismatch and vendor_account_mismatch rules existed — otherwise ~50%/100% of baseline data would false-positive on those rules respectively, making "sanity-check a few flagged results per rule" impossible to do meaningfully. Anomaly blocks override exactly the field each rule checks.
- Fixed a gap in this file's own Phase 7 checklist: "Debit/credit validation" was missing from the original list (design.docx lists it as one of the 10 rule modules) — added it back rather than leaving it undocumented.
- **Phase 8:** `docs/design.docx` and `docs/roadmap.docx` are plain UTF-8 text saved with a `.docx` extension, not real Office Open XML — `python-docx`/binary tooling fails on them (`PackageNotFoundError`). Read them as plain text instead. Worth renaming to `.txt` in a future session, but left as-is since the task only needed their content, not the extension fixed.
- **Phase 8:** Policy chunks are split by real chapter/clause numbering (e.g. "4.1.5"), not fixed-size windows, per the task requirement — `app/services/policy_chunking.py`. Footnote/citation text is deliberately dropped rather than kept (filtered out by point size, <9.5pt vs 10-12pt body text), since it's supplementary citation material, not operative policy text, and its numbered-footnote lines would otherwise be indistinguishable from real numbered clauses.
- **Phase 8:** `Policy.clause_ref`/`Policy.chapter` are new plain-string columns (migration `4ec0911d45e2`), unindexed beyond a plain btree index — mirrors the existing `audit_flags.rule_name` precedent of "plain indexed string, not an enum" so new documents with different numbering conventions don't need a migration.
- **Phase 8:** Chose **BAAI/bge-base-en-v1.5** over the task's suggested `bge-small` after verification showed `bge-small` ranked the correct answer clause for "what is the reporting threshold for fraud in NBFCs" at 19th out of 20 candidates. The task's wording ("e.g. BAAI/bge-small-en-v1.5") treats the model as a suggestion, not a hard requirement, and retrieval correctness on the task's own verification queries took priority. `bge-base` is still fully local/free via `sentence-transformers`, just larger (768-dim vs 384-dim).
- **Phase 8:** Embedding input is enriched with ancestor-clause headings and the chapter label (`_build_embedding_texts()` in `scripts/ingest_policies.py`), separate from the stored/returned `content` — a deeply nested clause is often a terse operative sentence with none of its section's topical vocabulary (e.g. clause 6.2.1 never uses the word "report"; its parent heading 6.2 is literally "Modalities of *Reporting* Incidents of Fraud to RBI"). This is a general context-enrichment technique validated against a second, differently-phrased query and a third query against an unrelated document (KYC), not a fix tailored to one query's exact wording.
- **Phase 8:** `policy_search_default_top_k` is 8, not 5 — chosen after verification showed correct-but-non-lexically-matching clauses (like 6.2.1 above) can land just outside a top-5 cutoff even with a good embedding model; 8 comfortably covers the task's verification queries while still being a reasonable default result-list size. `PolicySearchRequest.top_k` remains caller-overridable, 1-20.
- **Phase 8:** `ingest_policies.py` fully clears and rebuilds both `policies` (Postgres) and the Qdrant `policies` collection on every run rather than diffing — matches the existing `seed_transactions.py`/rule-engine precedent of "safe to re-run" scripts, and 322 chunks re-embed in well under a minute locally so there's no cost pressure to make it incremental.
- **Phase 9:** `starlette==0.38.6`/`sse-starlette==2.1.3` are now pinned in `requirements.txt` alongside `mcp==1.28.1` — installing plain `mcp` (or `mcp[cli]`) without a pin resolves to `starlette>=0.49`, which breaks `fastapi==0.115.0` at import time (`Router.__init__() got an unexpected keyword argument 'on_startup'`). If `fastapi` is ever upgraded past the `0.115.x` line, revisit whether this pin is still needed.
- **Phase 9:** MCP tools (`app/mcp/server.py`) do not require a JWT/`get_current_user` the way every HTTP endpoint does — there's no Authorization header in a tool call, and the MCP host process (whatever spawns the stdio server) is treated as the trust boundary instead. Revisit if the MCP server is ever exposed over a network transport rather than run locally via stdio.
- **Phase 9:** `flag_discrepancy`'s manual flags use `rule_name="manual"` (`risk_scoring.MANUAL_FLAG_RULE_NAME`) and always `risk_points=0`, deliberately not contributing to `transaction.risk_score`. `evaluate_transaction()` recomputes `risk_score` from `ACTIVE_RULES` alone on every call, so any non-zero points a manual flag tried to add would be silently wiped by the next `evaluate-all` — kept it a pure human-observation annotation instead of half-integrating it into a scoring model it isn't part of. Revisit only if manual flags are later given real point values with `evaluate_transaction()` also updated to preserve them.
- **Phase 9:** Extracted `app/services/ledger_service.py` (`search_transactions`, `get_transaction_by_id`) out of `app/api/transactions.py`, and moved `app/api/rule_engine.py`'s private `_open_flags` into `app/services/risk_scoring.py` as `get_open_flags` — both were previously inlined in route handlers with no way for the new MCP tools to reuse them without copy-pasting. The HTTP routes now just call these same functions; behavior is unchanged (re-verified end-to-end after the refactor).

---

## Setup note for Claude Code
Add this to your `CLAUDE.md` at the repo root so updates happen automatically:

```
At the end of every working session, update PROGRESS.md:
- Move any newly finished checklist items to [x]
- Add a dated bullet under "What's done"
- Rewrite "What's next" with the actual next 1-3 priorities
- Log any architecture/schema decisions that differ from the original design doc
```